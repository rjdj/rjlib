[patch]
name=m_accel_roll
description=watches the force sensor's output and calculates the rolling of the device (iPod, iPhone, ...).
related=m_accel_magnitude

[inlet 0]
domain=boolean
description=m_accel_roll watches the force sensor's output and calculates the pitching of the device (iPod, iPhone, ...).

[outlet 0]
domain=float
description=roll
