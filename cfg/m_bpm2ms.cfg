[patch]
name=m_bpm2ms
description=convert between a frequeny specified as BPM (beats per minute) to a period in milliseconds (and back).
related=m_beat2ms,m_ms2beat

[inlet 0]
domain=float
description=BPM or Period in milliseconds

[outlet 0]
domain=float
description=Period in milliseconds or BPM

