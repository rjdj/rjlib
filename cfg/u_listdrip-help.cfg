[patch]
name=u_listdrip
description=serialize a list immediately (same as [list-drip] from the [list]-abs collection).
related=list-drip, list

[inlet 0]
domain=message
type=list
description=message object containing list.

[outlet 0]
domain=message
description=same as Inlet 0.

[outlet 1]
domain=message
description=sends "bang" when list reaches to its end.




