[patch]
name=u_listfilter
description=[ returns a sequence consisting of those items from the sequence for which the user-defined operation cross-connected to the rightmost in- and outlet is true. Everything that is not 0 is considered a TRUE value. Internally [select 0] is used.related=


[inlet 0]
domain=float
description=message containing a list/sequence.

[inlet 1]
domain=
name=
type=float
description=cross-connected user-defined operation.
example=

[outlet 0]
domain=float
description=message containing filtered list.

[outlet 1]
domain=float
description=cross-connected user-defined operation.
