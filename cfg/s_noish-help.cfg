[patch]
name=s_noish
description=Generates a noise signal that is sampled at the specified frequency. This is similar to filtered noise, but sounds more 8-bit ish.

related=

[argument 0]
domain=
description=sample-and-hold frequency (optional).

[inlet 0]
domain=float
description=sample-and-hold frequency.

[outlet 0]
domain=audio
description=audio signal with sample and hold noise

