[patch]
name=u_dumpfile
description=u_dumpfile is a slightly enhanced clone of textfile that additionally supports dumping a file to its outlet, optionally numbered.
related=textfile

[argument 0]
description=default textfile to load(optional).
[argument 1]
description="cr" to use carriage returns for line endings.


[inlet 0]
domain=message
description=various dump messages, refer to example.

[outlet 0]
domain=
description=